product:
    type: lib
    platforms:
        - android
        - iosX64
        - iosArm64
        - iosSimulatorArm64

apply:
    - ../common.module-template.yaml

settings:
    android:
        namespace: app.reitan.tender.shared
    ios:
        framework:
            basename: Shared