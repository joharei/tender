{
	"configurations": [
		
		{
			"name": "jvmApp",
			"type": "gradle",
			"workingDir": "$PROJECT_DIR$",
			"tasks": ["jvmRun"],
			"args": ["-DmainClass=MainKt", "--quiet", "-p", "$PROJECT_DIR$/composeApp/jvmApp"],
			"initScripts": {
				"flmapper": "ext.mapPath = { path -> null }"
			}
		}
	]
}